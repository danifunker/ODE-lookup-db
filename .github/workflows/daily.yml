name: Daily scrape

on:
  schedule:
    - cron: "17 6 * * *"   # 06:17 UTC daily
  workflow_dispatch:
    inputs:
      limit:
        description: "Cap on new discs to fetch this run"
        required: false
        default: ""
      allow_deletions:
        description: "Permit row removals (default: warn only)"
        required: false
        default: "false"

permissions:
  contents: write
  issues: write

concurrency:
  group: db-write
  cancel-in-progress: false

jobs:
  scrape:
    runs-on: ubuntu-latest
    timeout-minutes: 330  # under the 6h GHA cap
    steps:
      - uses: actions/checkout@v5
        with:
          lfs: true

      - uses: astral-sh/setup-uv@08807647e7069bb48b6ef5acd8ec9567f424441b
        with:
          python-version: "3.12"

      - name: Install
        run: uv sync --extra dev

      - name: Process recheck requests
        env:
          GH_TOKEN: ${{ github.token }}
        run: uv run scripts/recheck.py

      - name: Scrape new discs
        run: |
          ARGS=""
          if [ -n "${{ inputs.limit }}" ]; then ARGS="--limit ${{ inputs.limit }}"; fi
          uv run scripts/scrape.py $ARGS

      - name: Validate
        run: uv run scripts/validate.py

      - name: Canary
        run: uv run scripts/canary.py

      - name: Refresh stats
        run: uv run scripts/stats.py

      - name: Tag previous HEAD for rollback
        run: |
          DATE=$(date -u +%Y-%m-%d)
          git tag -f "db-good-$DATE" HEAD
          git push --force origin "db-good-$DATE" || true

      - name: Commit JSONL + stats
        run: |
          git config user.name  "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add data/redump.jsonl data/stats.json
          if git diff --cached --quiet; then
            echo "no changes"
            exit 0
          fi
          git commit -m "data: daily scrape $(date -u +%Y-%m-%d)"
          git push

      - name: Build SQLite release artifact
        env:
          GITHUB_SHA: ${{ github.sha }}
        run: uv run scripts/build_sqlite.py

      - name: Validate SQLite artifact
        run: uv run scripts/validate_sqlite.py

      - name: Compress release artifact
        run: |
          sudo apt-get update -qq && sudo apt-get install -y -qq zstd
          zstd --ultra -22 --long=27 -f -o data/redump.sqlite.zst data/redump.sqlite
          sha256sum data/redump.sqlite      > data/redump.sqlite.sha256
          sha256sum data/redump.sqlite.zst  > data/redump.sqlite.zst.sha256
          ls -lh data/redump.sqlite data/redump.sqlite.zst

      - name: Publish release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          TAG="db-$(date -u +%Y-%m-%d)"
          ASSETS=(
            data/redump.sqlite.zst
            data/redump.sqlite.zst.sha256
            data/redump.sqlite.sha256
          )
          gh release create "$TAG" "${ASSETS[@]}" \
            --title "Database $TAG" \
            --notes "Automated daily build. Schema v1. Decompress with: zstd -d redump.sqlite.zst" || \
          gh release upload "$TAG" "${ASSETS[@]}" --clobber
          # Moving "latest" pointer for stable consumer URL
          gh release delete latest --yes --cleanup-tag || true
          gh release create latest "${ASSETS[@]}" \
            --title "Latest" \
            --notes "Always points at the most recent successful build. Decompress with: zstd -d redump.sqlite.zst"

      - name: File issue on warnings
        if: always()
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          if [ -f data/warnings.json ] && [ "$(jq '.warnings | length' data/warnings.json)" -gt 0 ]; then
            BODY=$(jq -r '.warnings | join("\n\n")' data/warnings.json)
            gh issue create \
              --title "Daily scrape warnings: $(date -u +%Y-%m-%d)" \
              --label "automated,warning" \
              --body "$BODY"
          fi

      - name: File issue on failure
        if: failure()
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh issue create \
            --title "Daily scrape FAILED: $(date -u +%Y-%m-%d)" \
            --label "automated,bug" \
            --body "Workflow run: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}"
